{
    "name": "hardhat-template",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "deploy": "npx hardhat run scripts/deploy.ts --network ",
        "deployVerify": "VERIFY=true npx hardhat run scripts/deploy.ts --network ",
        "xdeploy": "XDEPLOY=true npx hardhat xdeploy; rm ./deploy-args.ts",
        "interact": "npx hardhat run scripts/interact.ts",
        "fetch": "npx hardhat run scripts/fetch.ts",
        "pretty": "prettier --write .",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@openzeppelin/contracts": "^4.8.3",
        "axios": "^1.5.0",
        "crypto": "^1.0.1",
        "dotenv": "^16.0.3",
        "hardhat": "^2.14.0",
        "prettier": "^2.8.8",
        "xdeployer": "^1.2.5"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^1.0.2"
    }
}
